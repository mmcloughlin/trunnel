language: go
sudo: false
go:
  - 1.9.x
install:
  - go get -t ./...
  - go get github.com/go-playground/overalls
  - go get github.com/mattn/goveralls
script:
  - overalls -project=github.com/mmcloughlin/trunnel -- -v
  - goveralls -coverprofile=overalls.coverprofile -service=travis-ci
